
// A diferencia de JavaScript, TypeScript permite definir el tipo de dato de los parámetros o retorno de una función.
let suma = function (a: number, b: number): number {
    return a + b;
}

let suma2 = (a: number, b: number): number => {
    return a + b;
}